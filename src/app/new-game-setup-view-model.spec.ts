import { NewGameSetupViewModel } from './new-game-setup-view-model';

describe('NewGameSetupViewModel', () => {
  it('should create an instance', () => {
    expect(new NewGameSetupViewModel()).toBeTruthy();
  });
});
