import { OnBasePctPipePipe } from './on-base-pct-pipe.pipe';

describe('OnBasePctPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OnBasePctPipePipe();
    expect(pipe).toBeTruthy();
  });
});
