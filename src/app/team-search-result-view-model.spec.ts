import { TeamSearchResultViewModel } from './team-search-result-view-model';

describe('TeamSearchResultViewModel', () => {
  it('should create an instance', () => {
    expect(new TeamSearchResultViewModel()).toBeTruthy();
  });
});
