import { GameAtBatViewModel } from './game-at-bat-view-model';

describe('GameAtBatViewModel', () => {
  it('should create an instance', () => {
    expect(new GameAtBatViewModel()).toBeTruthy();
  });
});
