import { BattingAvgPipePipe } from './batting-avg-pipe.pipe';

describe('BattingAvgPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BattingAvgPipePipe();
    expect(pipe).toBeTruthy();
  });
});
