import { GameInningViewModel } from './game-inning-view-model';

describe('GameInningViewModel', () => {
  it('should create an instance', () => {
    expect(new GameInningViewModel()).toBeTruthy();
  });
});
