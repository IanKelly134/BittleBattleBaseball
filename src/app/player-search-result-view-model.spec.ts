import { PlayerSearchResultViewModel } from './player-search-result-view-model';

describe('PlayerSearchResultViewModel', () => {
  it('should create an instance', () => {
    expect(new PlayerSearchResultViewModel()).toBeTruthy();
  });
});
