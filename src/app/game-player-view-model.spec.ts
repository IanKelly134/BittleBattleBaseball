import { GamePlayerViewModel } from './game-player-view-model';

describe('GamePlayerViewModel', () => {
  it('should create an instance', () => {
    expect(new GamePlayerViewModel()).toBeTruthy();
  });
});
