import { SearchTeamsViewModel } from './search-teams-view-model';

describe('SearchTeamsViewModel', () => {
  it('should create an instance', () => {
    expect(new SearchTeamsViewModel()).toBeTruthy();
  });
});
