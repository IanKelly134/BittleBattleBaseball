import { PlayerSeasonViewModel } from './player-season-view-model';

describe('PlayerSeasonViewModel', () => {
  it('should create an instance', () => {
    expect(new PlayerSeasonViewModel()).toBeTruthy();
  });
});
